# task definitions for taskreg.cc

def task1(args, user_data, proc):
    print "hello from task1 args=({!r}) userdata=({!r}) proc=({:x})".format(args,
                                                                      user_data,
                                                                      proc)
